{
  "name": "api",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "node --watch --import ts-node/register/transpile-only src/index.ts",
    "build": "tsup src/index.ts --format cjs --dts",
    "start": "node dist/index.js",
    "lint": "eslint src/"
  },
  "dependencies": {
    "@fastify/cors": "^11.2.0",
    "fastify": "^5.7.4"
  },
  "devDependencies": {
    "@seoul-advanture/eslint-config": "workspace:*",
    "@seoul-advanture/typescript-config": "workspace:*",
    "@types/node": "^20.19.35",
    "ts-node": "^10.9.2",
    "tsup": "^8.5.1",
    "typescript": "^5.9.3"
  }
}